{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "skipFiles": ["<node_internals>/**"],
            "program": "${workspaceFolder}/index.js",  // Replace 'index.js' with your entry file
            "envFile": "${workspaceFolder}/.env"  // Optional: use if you have an environment file
        }
    ]
}
